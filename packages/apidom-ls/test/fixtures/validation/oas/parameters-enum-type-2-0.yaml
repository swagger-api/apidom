swagger: "2.0"
info:
  title: Enum Type Mismatch
  version: "1.0"
paths:
  /test:
    get:
      parameters:
        - name: status
          in: query
          required: true
          type: string
          enum: [1, 2, 3]
      responses:
        '200':
          description: OK
