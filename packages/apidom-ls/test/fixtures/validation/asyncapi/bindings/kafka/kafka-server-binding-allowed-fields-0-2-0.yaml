asyncapi: 3.0.0
info:
  title: Sample AsyncAPI
  version: '1.0.0'
servers:
  server1:
    host: example.com
    protocol: kafka
    bindings:
      kafka:
        foo: bar
        bindingVersion: '0.2.0'
