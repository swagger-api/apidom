# Build parallelization settings
# Adjust these based on your machine's CPU cores

# Number of packages Lerna builds in parallel (default: 8)
LERNA_CONCURRENCY=8

# Number of tasks per package to run in parallel (default: 6)
CPU_CORES=6

# Number of packages to test in parallel (default: 4)
LERNA_TEST_CONCURRENCY=4
