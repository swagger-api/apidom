openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /pets:
    get:
      responses:
        '200':
          description: ok
      parameters:
        - name: pathLevel
          in: query
          description: tags to filter by
          schema:
            type: string
        - name: pathLevel
          in: query
          description: tags to filter by
          schema:
            type: string
