asyncapi: 3.0.0
info:
  title: Sample AsyncAPI
  version: '1.0.0'
servers:
  server1:
    host: example.com:{port}
    pathname: /{path}
    protocol: http
    variables:
      port:
        $ref: '//'
      path:
        $ref: '#/servers/server1/variables/port'
