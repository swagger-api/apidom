openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /:
    post:
      responses:
        default:
          description: 'test'
      operationId: myId
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestBodies/MyBody/content/application~1json/schema'
components:
  requestBodies:
    MyBody:
      content:
        application/json:
          schema:
            type: string
