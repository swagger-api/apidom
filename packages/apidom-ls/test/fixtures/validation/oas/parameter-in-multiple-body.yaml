swagger: '2.0'
info:
  title: 'test'
  version: 1.0.0
paths:
  /:
    get:
      responses:
        default:
          description: 'test'
      consumes:
        - multipart/form-data
      parameters:
        - in: body
          name: Else1
          schema: {}
        - in: body
          name: Else2
          schema: {}
