{
  "openapi": "3.2.0",
  "paths": {
    "/users": {
      "query": {
        "summary": "Query users",
        "description": "Search for users using query parameters",
        "parameters": [
          {
            "$ref": "#/components/parameters/searchParam"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response"
          }
        }
      }
    }
  },
  "components": {
    "parameters": {
      "searchParam": {
        "name": "q",
        "in": "querystring",
        "required": false,
        "schema": {
          "type": "string"
        }
      }
    }
  }
}
