import { AsyncAPI3 } from '../target-specs.ts';

const documentation = [
  {
    docs: '#### [Operations Object](https://www.asyncapi.com/docs/reference/specification/v3.0.0#operationsObject)\n\nDescribes a map of operations which are used for sending and receiving messages. The map key represents the operationId and the map value is the definition of that operation.',
    targetSpecs: AsyncAPI3,
  },
];

export default documentation;
