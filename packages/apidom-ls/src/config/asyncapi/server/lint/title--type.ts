import { LinterMeta } from '../../../../apidom-language-types.ts';

const titleTypeLint: LinterMeta = {
  // add remaining docs here
  targetSpecs: [{ namespace: 'asyncapi', version: '3.0.0' }],
};

export default titleTypeLint;
