swagger: "2.0"
info:
  title: Enum Default Mismatch
  version: "1.0"
paths:
  /test:
    get:
      parameters:
        - name: fruit
          in: query
          type: string
          enum: ["apple", "orange"]
          default: "banana"
      responses:
        '200':
          description: OK
