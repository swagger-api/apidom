openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /foo:
    parameters:
      - $ref: '#/components/headers/MyHeader'
components:
  headers:
    MyHeader:
      description: ID of the user
      required: true
      schema:
        type: string

