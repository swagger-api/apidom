openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /foo:
    get:
      responses:
        '200':
          description: OK
          headers:
            X-MyHeader:
              $ref: 'https://api.github.com/users/github/orgs'
components:
  headers:
    MyHeader:
      description: ID of the user
      required: true
      schema:
        type: string
