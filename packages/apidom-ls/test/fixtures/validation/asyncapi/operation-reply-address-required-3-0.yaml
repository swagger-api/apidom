asyncapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
channels:
  testChannel:
    address: /test
operations:
  testOperation:
    action: send
    channel:
      $ref: '#/channels/testChannel'
    reply:
      address:
        # Missing required 'location' field - should trigger error
        description: Reply address description
      channel:
        $ref: '#/channels/testChannel'
