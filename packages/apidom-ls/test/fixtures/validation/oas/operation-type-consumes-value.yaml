swagger: "2.0"
info:
  version: 1.0.0
  title: 'test'
paths:
  /pets:
    get:
      consumes:
        - application/x-www-form-urlencoded
      responses:
        default:
          description: string
      parameters:
        - name: pathLevel
          type: file
          in: formData
          description: tags to filter by
