openapi: 3.1.0
info:
  version: '1.0'
  title: NOTCAMEL
paths:
  /NO_KEBAB/keb-ab/NOKEB2_NO:
    get:
      operationId: nokebab
  /keb-ab:
    get:
      operationId: kebab      
components:
  schemas:
    User:
      type: object
      properties:
        UPPERCASE:
          type: integer
        camelCase:
          type: integer          
  parameters:
    foo:
      in: query
      name: NonCAMEL
      schema:
        type: string
    foos:
      in: query
      name: camelCase
      schema:
        type: string      
    BAR:
      name: NonCAMEL
      in: query
      schema:
        type: string            
