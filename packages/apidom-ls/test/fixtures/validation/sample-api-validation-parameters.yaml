openapi: 3.1.0
info:
  title: Foo
  version: 0.1.0
paths:
  /foo/find:
    get:
      parameters:
        - name: fooId
          in: query
          schema:
            type: string
          style: label
        - name: name
          in: query
          schema:
            type: string
          style: simple
        - name: status
          in: query
          schema:
            type: string
          style: form
