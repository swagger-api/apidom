{
  "openapi": "3.1.0",
  "info": { "title": "Test", "version": "1.0.0" },
  "paths": {},
  "components": {
    "schemas": {
      "Concrete": {
        "$defs": {
          "payload": {
            "$dynamicAnchor": "payloadType",
            "type": "object",
            "properties": {
              "source": { "type": "string", "const": "concrete" }
            }
          }
        },
        "$ref": "#/components/schemas/Template"
      },
      "Template": {
        "$defs": {
          "payload": {
            "$dynamicAnchor": "payloadType",
            "not": {}
          }
        },
        "type": "object",
        "properties": {
          "payload": { "$dynamicRef": "#payloadType" }
        }
      }
    }
  }
}
