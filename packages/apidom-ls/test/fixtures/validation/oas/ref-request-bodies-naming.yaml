openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /:
    post:
      responses:
        default:
          description: 'test'
      operationId: myId
      requestBody:
        $ref: '#/components/headers/MyBody'
components:
  headers:
    MyBody:
      schema:
        description: 'description'
  requestBodies:
    MyBody:
      content:
        application/json:
          schema:
            type: string
