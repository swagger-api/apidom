import $refNoSiblingsLint from './$ref--no-siblings.ts';
import $refNoSiblingsReferenceLint from './$ref--no-siblings-reference.ts';

const lints = [$refNoSiblingsLint, $refNoSiblingsReferenceLint];

export default lints;
