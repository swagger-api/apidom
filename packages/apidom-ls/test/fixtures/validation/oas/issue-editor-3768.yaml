openapi: 3.1.0
info:
  title: deref
  version: 1.0.0
paths:
  /a:
    get:
      operationId: aget
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    post:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    put:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    head:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    delete:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    options:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
    trace:
      requestBody:
        description: test
        content:
          application/json:
            schema:
              type: string
  /b:
    get:
      operationId: aget
      requestBody:
        $ref: '#/components/requestBodies/test'              
components:
  requestBodies:
    test:
      description: test
      content:
        application/json:
          schema:
            type: string      
