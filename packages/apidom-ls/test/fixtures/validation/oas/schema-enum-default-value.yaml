openapi: 3.0.0
info:
  version: 1.0.0
  title: 'test'
paths:
  /pets:
    get:
      responses:
        default:
          description: 'test'
      parameters:
        - name: num
          in: query
          schema:
            type: number
            enum:
              - 1
              - 2
              - 3
            default: 0
