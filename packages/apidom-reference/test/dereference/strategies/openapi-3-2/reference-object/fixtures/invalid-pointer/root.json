{
  "openapi": "3.2.0",
  "components": {
    "parameters": {
      "userId": {
        "$ref": "invalid-pointer",
        "description": "override"
      },
      "userIdRef": {
        "name": "userId",
        "in": "query",
        "description": "ID of the user",
        "required": true
      }
    }
  }
}
