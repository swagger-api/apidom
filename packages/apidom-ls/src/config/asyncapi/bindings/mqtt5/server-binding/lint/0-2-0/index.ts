import allowedFieldsLint from './allowed-fields.ts';
import sessionExpiryIntervalTypeLint from './session-expiry-interval--type.ts';

const lints = [allowedFieldsLint, sessionExpiryIntervalTypeLint];

export default lints;
