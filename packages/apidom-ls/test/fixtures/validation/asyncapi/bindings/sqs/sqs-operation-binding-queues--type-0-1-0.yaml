asyncapi: 3.0.0
info:
  title: Sample AsyncAPI
  version: '1.0.0'
channels:
  channel1:
    address: example
operations:
  operation1:
    action: receive
    channel:
      $ref: '#/channels/channel1'
    bindings:
      sqs:
        queues: foo
        bindingVersion: '0.1.0'
