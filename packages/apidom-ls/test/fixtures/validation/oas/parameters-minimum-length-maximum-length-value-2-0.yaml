swagger: "2.0"
info:
  title: MinLength vs MaxLength Error
  version: "1.0"
paths:
  /test:
    get:
      parameters:
        - name: username
          in: query
          type: string
          minLength: 10
          maxLength: 5
      responses:
        '200':
          description: OK
