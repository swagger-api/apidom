swagger: "2.0"
info:
  title: Minimum vs Maximum Error
  version: "1.0"
paths:
  /test:
    get:
      parameters:
        - name: age
          in: query
          type: integer
          minItems: 50
          maxItems: 30 
      responses:
        '200':
          description: OK
